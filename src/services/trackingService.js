export const recordReadTime = (postId, timeInSeconds) => {
    // Mock implementation to prevent build errors
    console.log(`Mock: Recorded read time for ${postId}: ${timeInSeconds}s`);
};

export const trackCategoryInteraction = (categoryId) => {
    // Mock implementation
    console.log(`Mock: Tracked category interaction: ${categoryId}`);
};
